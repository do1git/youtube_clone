extends base

block content 
    div 
        button#startBtn start Recording
    video#preview(style="width:200px")
    if errorMessage 
        span=errorMessage
    form(method="POST",enctype="multipart/form-data", action="/videos/upload")
        div
            label(for="video") Video File 
            input(id="video", type="file" ,accept="video/*", name="video", required)
        div
            label(for="thumb") Thumbnail File 
            input(id="thumb", type="file" ,accept="image/*", name="thumb", required)
        input(name="title", placeholder="Title", required, type="text",maxlength=80)
        input(name="description", placeholder="Description", required, type="text",minlength=20)
        input(name="hashtags", placeholder="Hashtags, seperated by comma.", required, type="text")
        input(type="submit", value="Upload Video")
block scripts
    script(src="/static/js/recorder.js")